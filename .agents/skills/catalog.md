# Skills Catalog — newsletter-crawler

> Auto-generated by `scripts/gen-catalog.mjs` (do not hand-edit). Every implementation task goes through `project-router`.

## knowledge

- **calling-the-llm-layer** — OpenRouter and DeepSeek V4 integration for the LLM layer - model slugs, reasoning effort xhigh/high (never max), json_schema strict plus zod validation, defensive JSON parse, the two-tier Pro/Flash strategy, and the .env key override. Use whenever you touch the LLM layer (src/llm.js), change prompts, schemas, reasoning, or models, or debug an OpenRouter 400 or 401 error.
- **fetching-and-extracting** — HTML fetching and content extraction - the fetchSmart static to Playwright rendering decision, DOM pruning, Readability first article extraction, robots politeness, the circuit breaker, and CSS selector validation thresholds. Use whenever you edit src/fetch.js, src/clean.js, or src/selectors.js, or change Playwright scrolling, rendering, robots, pruning, or selector validation.
- **following-code-style** — Coding conventions and code style for this repo - ESM modules and import order, logging via util log/warn/errorLog, defensive try/catch parsing, the single db.js stmts object, camelCase/SCREAMING_SNAKE naming, and the no-axios rule. Use whenever you write or edit JavaScript under src/, add a module, or review a diff for style, even if the user does not mention style.
- **persisting-and-orchestrating** — SQLite data layer and crawl orchestration - schema and tables, the prepared statements (stmts), the frontier state machine (pending, in_progress, done, failed) with resume, dedup via normalizeUrl and content_hash, the concurrency loop, and pagination stop conditions. Use whenever you edit src/db.js, src/crawl.js, or src/index.js, or change the schema, the frontier or queue, dedup, or pagination.

## task

- **extending-the-crawler** — How to extend the crawler following its conventions - add a newsletter source (edit config/sources.json or the add command), add a new src/ module, add a CLI command, register a publication, or add a new fetch/extract strategy. Use whenever you add a source, feature, module, command, or new capability to the crawler.
- **running-and-verifying-crawls** — How to run, smoke test, and verify the crawler end to end - npm run status, npm run crawl with --max-pages and --max-articles bounds, the cache-hit re-run check, DB inspection, markdown export, and the OpenRouter key probe. Use whenever you need to run the app, test a change, reproduce a crawl, or verify a fix works.

## Tooling
- Lint all skills: `node .agents/skills/scripts/skill-lint.mjs --all`
- Validate/evaluate a skill (writes token): `node .agents/skills/scripts/skill-eval.mjs <name>`
- Evolve a skill safely: `node .agents/skills/scripts/validate-skill.mjs <name>` (promotes SKILL.md.next)
- Regenerate this catalog: `node .agents/skills/scripts/gen-catalog.mjs`
